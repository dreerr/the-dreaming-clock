# The Dreaming Clock — 4 MB flash, dual OTA slots.
#
# 1.625 MB per app slot (default.csv gives 1.31 MB; the firmware was at 78.5% of that)
# 640 KB LittleFS for the web frontend (~15 KB after the esbuild+gzip step).
#
# CHANGING THIS REQUIRES A ONE-TIME USB FLASH — OTA cannot rewrite the partition table.
#
# The filesystem partition must be NAMED "spiffs" even though it holds
# LittleFS: Arduino's LittleFS.begin() looks that label up by default, and
# esp_littlefs fails with "partition 'spiffs' could not be found" otherwise.
#
# Name,     Type, SubType,  Offset,   Size,
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xE000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x1A0000,
app1,       app,  ota_1,    0x1B0000, 0x1A0000,
spiffs,     data, spiffs,   0x350000, 0xA0000,
coredump,   data, coredump, 0x3F0000, 0x10000,
